{
  "name": "leena",
  "private": true,
  "workspaces": [
    "app",
    "server",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:app": "npm run start --workspace app"
  }
}
